
type - link:css - link a css file.
directly above the </head>
type - script - link a script

properties applied to element - color, background, size etc.
selectors - connecting elements to blocks of CSS
property (background-color): - value (e.g. green)
property - value
element selector e.g. [h1] color, fontsize
ID selector [#] - #aboutme - for styling specific HTML elements only.
Class selector - multiple elements
Group selectors e.g. h1, h2
Class attribute on e.g. a [p] tag can apply multiple css selectors.

developer tools > Elements